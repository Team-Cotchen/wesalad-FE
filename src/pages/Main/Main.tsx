import Nav from 'components/Nav';
import React, { FunctionComponent } from 'react';

const Main: FunctionComponent = () => {
  return (
    <div>
      <Nav />
    </div>
  );
};

export default Main;
